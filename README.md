# nvim
```
cd ~./config/nvim # Hark, for 'tis here Nvim's secrets oft reside
git clone https://github.com/house-intellect/nvim.git # Fetch this hoard, and in this fold abide
cp ./JetBrainsMonoNerdFontMono-Regular.ttf /usr/share/fonts/ # Convey the font where Linux doth provide
fc-cache -f -v # Renew the font's cache, let all be espied
nvim # Now, revel as a champion, in pride
```